[pytest]
# When running from testing/appium, tests are under the tests/ folder
testpaths = tests

markers =
    smoke: mark a test as smoke
    regression: mark a test as regression
    flaky: mark a test as flaky (use sparingly)

# Default options for local runs: quiet output and stop after 1 failure
addopts = -q --maxfail=1
