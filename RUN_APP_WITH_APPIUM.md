1) Build debug APK:
   ./gradlew assembleDebug
2) Start emulator (or let Appium launch it):
   emulator -avd <AVD_NAME>
3) Start Appium server:
   appium
4) Run test using desired capabilities in appium/DESIRED_CAPABILITIES.md
